import React from "react";

function Header() {
  return (
    <header>
      <nav>
        <button>Home</button>
        <button>About</button>
        <button>Projects</button>
        <button>Skills</button>
      </nav>
    </header>
  );
}

export default Header;
